const locale = 'ru';

// work
// console.log(require('moment/locale/ru'));

// didn't work
// console.log(require(`moment/locale/${locale}`));

// didn't work
// (async () => { console.log(await(import(`moment/locale/${locale}`))); })();

export default () => '';
